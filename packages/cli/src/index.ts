import type { YApiFlowConfig } from '@yapi-flow/shared'

/**
 * Factory function for config type inference
 */
export function defineConfig(config: YApiFlowConfig): YApiFlowConfig {
  return config
}
