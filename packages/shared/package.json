{
  "name": "@yapi-flow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for YApi Flow",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
